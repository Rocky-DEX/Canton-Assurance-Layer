# Prebuilt images on the GitHub Container Registry, so a self-hoster can pull
# instead of compiling Rust and Next.js on their own machine.
#
#   ghcr.io/rocky-dex/canton-assurance-web
#   ghcr.io/rocky-dex/canton-assurance-service
#   ghcr.io/rocky-dex/canton-sim
#
# A tag `vX.Y.Z` publishes `X.Y.Z` and `latest`; every push to main publishes
# `edge`; every build also carries the commit sha. DEPLOY.md shows how to run
# them with docker-compose.ghcr.yml.
name: release

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  images:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: canton-assurance-web
            dockerfile: web/Dockerfile
          - name: canton-assurance-service
            dockerfile: rust/solvency-service/Dockerfile
          - name: canton-sim
            dockerfile: rust/sim-server/Dockerfile
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          # The registry namespace is the lowercase organisation name.
          images: ghcr.io/${{ github.repository_owner }}/${{ matrix.name }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=edge,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,format=short
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,scope=${{ matrix.name }},mode=max
